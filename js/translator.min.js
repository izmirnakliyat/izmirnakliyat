document.addEventListener('DOMContentLoaded', function() { console.log('Translator.js yüklendi'); const translator = { initialized: false, translationFixes: { 'communication': 'Contact', 'Communication': 'Contact', 'COMMUNICATION': 'CONTACT', 'iletişim': 'Contact', 'İletişim': 'Contact', 'İLETİŞİM': 'CONTACT', 'contact': 'Contact', 'CONTACT': 'CONTACT' }, init: function() { console.log('Translator başlatılıyor...'); this.hideGoogleTranslateElements(); this.createLanguageDropdown(); this.bindLanguageButtons(); this.checkGoogleTranslateAPI(); this.startTranslationFixes(); this.observeGoogleTranslateDom(); this.initialized = true; }, observeGoogleTranslateDom: function() { if (this._gtDomObserver) { return; } this._gtDomObserver = new MutationObserver(() => { this.hideGoogleTranslateElements(); if (localStorage.getItem('preferredLanguage') === 'en') { this.scheduleTranslationFixes(); } }); this._gtDomObserver.observe(document.body, { childList: true, subtree: true, attributes: true, attributeFilter: ['style', 'class'] }); }, scheduleTranslationFixes: function() { if (this._fixDebounceTimer) { clearTimeout(this._fixDebounceTimer); } this._fixDebounceTimer = setTimeout(() => { this.applyTranslationFixes(); }, 400); }, hideGoogleTranslateElements: function() { const banner = document.querySelector('.goog-te-banner-frame'); if (banner) { banner.style.display = 'none'; banner.style.visibility = 'hidden'; } const widget = document.querySelector('.goog-te-gadget'); if (widget) { widget.style.fontSize = '0'; widget.style.display = 'none'; } const translateBar = document.querySelector('.skiptranslate'); if (translateBar) { translateBar.style.display = 'none'; translateBar.style.visibility = 'hidden'; } document.body.style.top = '0px !important'; const htmlElement = document.querySelector('html'); if (htmlElement && htmlElement.style.position === 'relative') { htmlElement.style.position = 'static'; } }, checkGoogleTranslateAPI: function() { let attempts = 0; const maxAttempts = 20; const poll = () => { if (typeof google !== 'undefined' && google.translate) { console.log('Google Translate API hazır'); this.hideGoogleTranslateElements(); const activeLang = localStorage.getItem('preferredLanguage') || 'tr'; this.updateDropdownFlag(activeLang); return; } attempts += 1; if (attempts < maxAttempts) { setTimeout(poll, 500); } }; poll(); }, bindLanguageButtons: function() { console.log('Dil butonları bağlanıyor...'); const langButtons = document.querySelectorAll('.lang-switcher a, .mobile-lang a, .lang-dropdown-content a'); console.log('Bulunan dil butonları:', langButtons.length); langButtons.forEach(button => { const newButton = button.cloneNode(true); if (button.parentNode) { button.parentNode.replaceChild(newButton, button); } }); const refreshedButtons = document.querySelectorAll('.lang-switcher a, .mobile-lang a, .lang-dropdown-content a'); refreshedButtons.forEach(button => { button.addEventListener('click', (e) => { e.preventDefault(); e.stopPropagation(); const langCode = button.getAttribute('data-lang'); if (langCode) { console.log('Dil değiştiriliyor:', langCode); const currentLang = localStorage.getItem('preferredLanguage'); if (currentLang === langCode) { console.log('Zaten seçili dil:', langCode); return; } refreshedButtons.forEach(btn => btn.classList.remove('active')); button.classList.add('active'); const dropdownContent = document.querySelector('.lang-dropdown-content'); if (dropdownContent) { dropdownContent.classList.remove('show'); } this.translatePage(langCode); } }); }); document.addEventListener('click', (e) => { const btnElement = document.querySelector('.lang-dropdown-btn'); if (btnElement && btnElement.contains(e.target)) { e.preventDefault(); e.stopPropagation(); const content = document.querySelector('.lang-dropdown-content'); if (content) { content.classList.toggle('show'); } } else if (!document.querySelector('.lang-dropdown-content')?.contains(e.target)) { const content = document.querySelector('.lang-dropdown-content'); if (content && content.classList.contains('show')) { content.classList.remove('show'); } } }); const activeLang = localStorage.getItem('preferredLanguage') || 'tr'; const activeButtons = document.querySelectorAll(`.lang-switcher a[data-lang="${activeLang}"], .mobile-lang a[data-lang="${activeLang}"], .lang-dropdown-content a[data-lang="${activeLang}"]`); activeButtons.forEach(btn => btn.classList.add('active')); this.updateDropdownFlag(activeLang); }, createLanguageDropdown: function() { console.log("Dil dropdown menüsü oluşturuluyor"); const langSwitcher = document.querySelector('.lang-switcher'); if (!langSwitcher) return; const langButtons = langSwitcher.querySelectorAll('a'); if (langButtons.length === 0) return; const langDropdown = document.createElement('div'); langDropdown.className = 'lang-dropdown'; const dropdownBtn = document.createElement('div'); dropdownBtn.className = 'lang-dropdown-btn'; const activeLang = localStorage.getItem('preferredLanguage') || 'tr'; let activeFlag = ''; let activeAlt = ''; langButtons.forEach(btn => { if (btn.getAttribute('data-lang') === activeLang) { const img = btn.querySelector('img'); if (img) { activeFlag = img.src; activeAlt = img.alt; } } }); if (!activeFlag && langButtons[0]) { const img = langButtons[0].querySelector('img'); if (img) { activeFlag = img.src; activeAlt = img.alt; } } dropdownBtn.innerHTML = ` <img src="${activeFlag}" alt="${activeAlt}" id="active-flag"> <i class="fas fa-chevron-down"></i> `; langDropdown.appendChild(dropdownBtn); const dropdownContent = document.createElement('div'); dropdownContent.className = 'lang-dropdown-content'; langButtons.forEach(btn => { const langCode = btn.getAttribute('data-lang'); const img = btn.querySelector('img'); if (langCode && img) { const newBtn = document.createElement('a'); newBtn.href = '#'; newBtn.setAttribute('data-lang', langCode); newBtn.innerHTML = ` <img src="${img.src}" alt="${img.alt}"> <span style="display: none;">${img.alt}</span> `; if (langCode === activeLang) { newBtn.classList.add('active'); } dropdownContent.appendChild(newBtn); } }); langDropdown.appendChild(dropdownContent); langSwitcher.replaceWith(langDropdown); }, updateDropdownFlag: function(langCode) { console.log('Bayrak güncelleniyor:', langCode); const activeFlag = document.getElementById('active-flag'); if (!activeFlag) { console.error('active-flag ID\'li eleman bulunamadı'); return; } const allButtons = document.querySelectorAll('a[data-lang]'); let flagFound = false; allButtons.forEach(btn => { if (btn.getAttribute('data-lang') === langCode) { const img = btn.querySelector('img'); if (img && img.src) { activeFlag.src = img.src; activeFlag.alt = img.alt || langCode; console.log('Bayrak bulundu ve güncellendi:', langCode, img.src); flagFound = true; } } }); if (!flagFound) { const langButton = document.querySelector(`.lang-dropdown-content a[data-lang="${langCode}"]`); if (langButton) { const img = langButton.querySelector('img'); if (img && img.src) { activeFlag.src = img.src; activeFlag.alt = img.alt || langCode; console.log('Dropdown içinde bayrak bulundu:', langCode, img.src); flagFound = true; } } } if (!flagFound) { const flagMap = { 'tr': 'https://cdn.countryflags.com/thumbs/turkey/flag-400.png', 'en': 'https://cdn.countryflags.com/thumbs/united-kingdom/flag-400.png', 'ar': 'https://cdn.countryflags.com/thumbs/saudi-arabia/flag-400.png', 'fr': 'https://cdn.countryflags.com/thumbs/france/flag-400.png', 'de': 'https://cdn.countryflags.com/thumbs/germany/flag-400.png', 'es': 'https://cdn.countryflags.com/thumbs/spain/flag-400.png', 'ru': 'https://cdn.countryflags.com/thumbs/russia/flag-400.png' }; if (flagMap[langCode]) { activeFlag.src = flagMap[langCode]; activeFlag.alt = langCode; console.log('CDN adreslerinden bayrak kullanıldı:', langCode, flagMap[langCode]); flagFound = true; } } this.updateMobileLanguageButton(langCode); }, updateMobileLanguageButton: function(langCode) { const mobileLang = document.querySelector('.mobile-lang'); if (!mobileLang) return; const mobileButtons = mobileLang.querySelectorAll('a[data-lang]'); mobileButtons.forEach(btn => { btn.classList.remove('active'); }); mobileButtons.forEach(btn => { const btnLangCode = btn.getAttribute('data-lang'); if (btnLangCode === langCode) { btn.classList.add('active'); console.log('Mobil menüde aktif dil işaretlendi:', langCode); } }); if (mobileButtons.length === 0 || !mobileLang.querySelector('.active')) { console.log('Mobil dil butonları yeniden oluşturuluyor'); mobileLang.innerHTML = ''; const langButtons = document.querySelectorAll('.lang-dropdown-content a'); langButtons.forEach(btn => { const btnLangCode = btn.getAttribute('data-lang'); const img = btn.querySelector('img'); if (btnLangCode && img) { const newBtn = document.createElement('a'); newBtn.href = '#'; newBtn.setAttribute('data-lang', btnLangCode); if (btnLangCode === langCode) { newBtn.classList.add('active'); } newBtn.innerHTML = `<img src="${img.src}" alt="${img.alt || btnLangCode}">`; mobileLang.appendChild(newBtn); } }); this.bindLanguageButtons(); } }, translatePage: function(langCode) { if (!langCode) return; const previousLang = localStorage.getItem('preferredLanguage') || 'tr'; if (previousLang === langCode) { console.log('Aynı dil zaten seçili:', langCode); return; } console.log('Dil değiştiriliyor:', previousLang, '->', langCode); localStorage.setItem('preferredLanguage', langCode); this.updateDropdownFlag(langCode); try { const cookieDomain = window.location.hostname; if (langCode === 'tr') { console.log('Türkçe diline dönülüyor, çerezler temizleniyor'); document.cookie = 'googtrans=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;'; document.cookie = 'googtrans=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/; domain=' + cookieDomain; document.cookie = 'googtrans=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/; domain=.' + cookieDomain; location.reload(); return; } document.cookie = 'googtrans=/tr/' + langCode + '; path=/;'; document.cookie = 'googtrans=/tr/' + langCode + '; path=/; domain=' + cookieDomain; document.cookie = 'googtrans=/tr/' + langCode + '; path=/; domain=.' + cookieDomain; if (typeof google !== 'undefined' && google.translate) { const select = document.querySelector('.goog-te-combo'); if (select) { select.value = langCode; select.dispatchEvent(new Event('change')); console.log('API ile çeviri aktifleştirildi'); setTimeout(() => { this.hideGoogleTranslateElements(); this.applyTranslationFixes(); }, 300); } else { console.log('Google Translate combosuna erişilemedi, sayfa yenileniyor'); location.reload(); } } else { console.log('Google Translate API mevcut değil, sayfa yenileniyor'); location.reload(); } } catch (error) { console.error('Dil değiştirme hatası:', error); location.reload(); } }, startTranslationFixes: function() { setTimeout(() => { this.applyTranslationFixes(); }, 2000); }, applyTranslationFixes: function() { const currentLang = localStorage.getItem('preferredLanguage'); if (currentLang === 'en') { console.log('Çeviri düzeltmeleri uygulanıyor...'); const textElements = document.querySelectorAll('h1, h2, h3, h4, h5, h6, p, span, a, button, label, div, li, td, th'); textElements.forEach(element => { if (element.childNodes.length === 1 && element.childNodes[0].nodeType === Node.TEXT_NODE) { let text = element.textContent; let originalText = text; Object.keys(this.translationFixes).forEach(wrongText => { const correctText = this.translationFixes[wrongText]; const regex = new RegExp(wrongText, 'gi'); text = text.replace(regex, correctText); }); if (element.tagName && (element.tagName.match(/^H[1-6]$/) || element.closest('nav') || element.closest('.menu'))) { text = text.replace(/\bcontact\b/gi, 'Contact'); } if (text !== originalText) { element.textContent = text; console.log('Çeviri düzeltildi:', originalText, '->', text); } } else { this.fixTextInElement(element); } }); } }, fixTextInElement: function(element) { const walker = document.createTreeWalker( element, NodeFilter.SHOW_TEXT, null, false ); const textNodes = []; let node; while (node = walker.nextNode()) { textNodes.push(node); } textNodes.forEach(textNode => { let text = textNode.textContent; let originalText = text; Object.keys(this.translationFixes).forEach(wrongText => { const correctText = this.translationFixes[wrongText]; const regex = new RegExp(wrongText, 'gi'); text = text.replace(regex, correctText); }); const parentElement = textNode.parentElement; if (parentElement && (parentElement.tagName && parentElement.tagName.match(/^H[1-6]$/) || parentElement.closest('nav') || parentElement.closest('.menu') || parentElement.closest('.lang-switcher') || parentElement.closest('.mobile-lang'))) { text = text.replace(/\bcontact\b/gi, 'Contact'); } if (text !== originalText) { textNode.textContent = text; console.log('Text node düzeltildi:', originalText, '->', text); } }); } }; translator.init(); window.translator = translator; });